import 'package:flutter/material.dart';

class Trainerwithoutschedulepage extends StatelessWidget {
  const Trainerwithoutschedulepage({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      mainAxisAlignment: MainAxisAlignment.center,
      children: [
        Text('Have a Chat with trainer until he Schdule the workout plans', style: TextStyle(color: Colors.white, fontSize: 18),),
      ],
    );
  }
}
